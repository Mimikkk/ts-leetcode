import { exercise } from "@shared/utilities/exercise.js";
import TestCase from "./day-23-walk.case.txt?raw";
import UserCase from "./day-23-walk.user.txt?raw";

const walk = (input: string): number => 0;

exercise(walk, [
  [[TestCase], 5],
  [[UserCase], 3617],
]);
