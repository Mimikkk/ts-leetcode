import { exercise } from "@shared/utilities/exercise.js";
import TestCase from "./5-fertilizer.case.txt?raw";

const fertilizer = (input: string): number => {
  return 0;
};

exercise(fertilizer, [
  [[TestCase], 13],
  // [[UserCase], 18519],
]);
